# Pehle omegaconf manually install karo older pip se
pip install pip==23.3.2
pip install omegaconf==2.0.6
pip install rvc-python --no-deps
pip install faiss-cpu torch torchaudio
```

---

## Ya Seedha Yeh Karo — Carryminati/JARVIS Voice

**weights.gg** pe jao aur search karo:
```
JARVIS RVC
```
Ya koi bhi Indian male voice download karo — `.pth` aur `.index` file milegi

Phir RVC WebUI mein:
```
Input Audio: ChatTTS ka output.wav
Voice Model: jarvis.pth
Convert → Download
```

---

## Honestly Meri Suggestion
```
rvc-python → Bahut buggy on Windows ❌
RVC WebUI  → Works perfectly on Windows ✅